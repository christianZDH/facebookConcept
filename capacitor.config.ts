import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'facebookredesign',
  webDir: 'dist/facebookredesign',
  bundledWebRuntime: false,
};

export default config;
